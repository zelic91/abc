{
  "name": "simpledash",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "sass --watch main.scss:main.css",
    "build-landing": "sass --no-source-map --watch landing.scss:landing.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.6.0",
    "feather-icons": "^4.28.0",
    "jquery": "^3.6.0"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
